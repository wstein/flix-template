[package]
name        = "flix-template"
description = "A Flix project template that carries its own compiler via flixw"
version     = "0.1.0"
flix        = "0.75.2"
license     = "Apache-2.0"
authors     = ["Werner Stein <werner.stein@gmail.com>"]
